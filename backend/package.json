{
  "name": "clinic-booking-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Clinic booking backend (Node + Express + MongoDB)",
  "main": "src/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --exec node src/server.js",
    "lint": "echo \"Add lint command (eslint) if needed\"",
    "test": "echo \"Add tests (jest)\" && exit 0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-validator": "^7.3.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.0.0",
    "morgan": "^1.10.1",
    "winston": "^3.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "author": "your-name",
  "license": "MIT"
}
